{
  "name": "restioo-vn",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test:legacy": "node --test tests/*.mjs",
    "test:webapp": "npm --prefix webapp test -- --run",
    "test": "npm run test:legacy && npm run test:webapp",
    "build": "npm --prefix webapp run build"
  }
}
